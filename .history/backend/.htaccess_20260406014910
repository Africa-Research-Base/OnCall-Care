<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent rewrite loops once we are already inside /public.
    RewriteCond %{REQUEST_URI} !^/public/

    # Route all requests through Laravel's /public entrypoint.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>